# loan-approval-prediction-logistic-regression
A machine learning project built in Orange Data Mining that predicts loan approval outcomes using Logistic Regression. The workflow includes data import, feature selection, model training, and performance evaluation through cross-validation.
